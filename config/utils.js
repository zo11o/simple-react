function cssLoaders(options) {
    options = options || {}
    const cssLoaders = {
        loader: 'css-loader',
        options: {
            sourceMap: options.sourceMap
        }
    }

    // function generateLoaders(loader, loaderOptions) {
    //     const 
    // }
}

module.exports = {
    cssLoaders
}